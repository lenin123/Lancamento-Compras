/** `.env.ts` is generated by the `npm run env` command */
import env from './.env';

/** environment de homologação */
export const environment = {
  production: true,
  version: env.npm_package_version,
  serverUrl: '',
  apiVersion: '1',
  envName: 'HML',
  API_URL: 'assets/api'
};
